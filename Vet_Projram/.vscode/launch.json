{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Executable (Eclipse Build)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/Debug/Vet_Projram.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Eclipse: Build Debug"
        }
    ]
}
